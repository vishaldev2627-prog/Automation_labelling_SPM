fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
python-multipart==0.0.9
opencv-python-headless==4.10.0.84
numpy==1.26.4
Pillow==10.4.0
PyYAML==6.0.2

# PyTorch (install the CUDA build matching your system from https://pytorch.org/get-started/locally/
# if the default pip wheel below does not include GPU support)
torch==2.4.1
torchvision==0.19.1

# SAM 2 (Meta) - install from source, see README.md for exact commands:
#   git clone https://github.com/facebookresearch/sam2.git
#   pip install -e ./sam2
# Left commented here because it is not a standard PyPI package name across versions.
# sam-2

# Optional ONNX runtime backend (CPU or GPU) if SAM_USE_ONNX=true in .env
onnxruntime-gpu==1.19.2 ; sys_platform == "linux"
