fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
python-multipart==0.0.9
# NOTE: ultralytics (below) depends on the full opencv-python, which conflicts with
# opencv-python-headless (both provide the `cv2` module and can't coexist). Let
# ultralytics pull in opencv-python instead of pinning headless here.
# numpy is left unpinned too so pip can pick one version satisfying both
# torch==2.5.1 and ultralytics rather than fighting a stale pin.
Pillow==10.4.0
PyYAML==6.0.2

# Annotation state / audit history storage (Phase 1a hardening - see
# annotation_module_build_plan.md). psycopg (v3) rather than psycopg2: it
# ships aarch64 wheels via the [binary] extra, needed for this host's
# Grace Blackwell build (see the Dockerfile's torch install comment).
SQLAlchemy==2.0.35
alembic==1.13.3
psycopg[binary]==3.2.3

# S3/MinIO client for publishing dataset snapshots to the staging store
# (app/services/object_store.py). Imported lazily, so a deployment that never
# publishes does not need it at runtime - but it is pinned here because the
# publish path is part of the shipped feature, not an optional extra.
# boto3 rather than the `minio` SDK: MinIO speaks S3, so pointing at real S3
# later is an endpoint change instead of a rewrite.
boto3==1.35.36

# Detector training/inference (fine-tunes a YOLOv8 model on your corrected
# annotations; see app/services/detector_service.py)
ultralytics

# MLflow tracking client (M5, Scope A only - see app/config.py's
# mlflow_tracking_uri comment). mlflow-skinny, not the full `mlflow` package:
# it's the tracking/registry client API only (mlflow.start_run, log_param,
# log_metric, log_artifact, MlflowClient) without the server/UI/full
# visualization dependency tree - the actual server runs as its own
# container from the official image (see mlflow/Dockerfile), so this
# service never needs those extras and the lower dependency footprint means
# less risk of conflicting with the delicate torch/ultralytics pins above.
mlflow-skinny==2.16.2

# PyTorch is installed separately in the Dockerfile, from PyTorch's own CUDA
# wheel index (torch==2.7.1+cu128 / torchvision==0.22.1+cu128), before this
# file is installed — see the Dockerfile for why. Left out of this file so a
# plain `pip install -r requirements.txt` (e.g. local dev without a matching
# GPU) doesn't fight that pin; install torch yourself for local dev, matching
# sam2's own "torch>=2.5.1" requirement (see sam2/setup.py).

# SAM 2 (Meta) - install from source, see README.md for exact commands:
#   git clone https://github.com/facebookresearch/sam2.git
#   pip install -e ./sam2
# Left commented here because it is not a standard PyPI package name across versions.
# sam-2

# Optional ONNX runtime backend (CPU or GPU) if SAM_USE_ONNX=true in .env
# No aarch64 wheel is published for this pin, so it's restricted to x86_64 hosts.
onnxruntime-gpu==1.19.2 ; sys_platform == "linux" and platform_machine == "x86_64"
