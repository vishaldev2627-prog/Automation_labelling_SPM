# Loaded into the backend container by docker-compose.yml (env_file: .env).
# Dataset/exports/models/logs live in Docker-managed named volumes now, so
# nothing here is required for a default deploy. Add optional backend
# overrides below if you need them (see backend/.env.example for the full
# list) — e.g. CORS_ORIGINS, SAM_DEVICE, LOG_LEVEL.

